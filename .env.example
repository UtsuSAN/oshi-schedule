# Local SQLite database. Copy this file to .env for local configuration.
DATABASE_URL=sqlite:///./data/oshi_schedule.db

# Base URL printed by the CLI for the Candidate list; this does not configure server binding.
APP_BASE_URL=http://127.0.0.1:8000

# Optional; only required for an explicit X API fetch from the CLI.
X_BEARER_TOKEN=
